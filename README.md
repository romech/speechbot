# speechbot

*Written fully at First Line Software Hackaton.*

Telegram bot that transforms voice recordings. Not by itself, but using an API.

To make it actually work you need:

1. server with appropriate endpoints
2. `pip install pyTelegramBotAPI, requests[socks], PyYAML `
3. config file with settings for telegram bot and server.
